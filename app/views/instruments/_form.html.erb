<%= form_for @instrument do |o| %>
    <%= o.label "Name" %>
    <%= o.text_field :name %><br>

    <%= o.label "Positions" %><br>
    <%= o.collection_check_boxes :position_ids, Position.all, :id, :title %><br>

    <%= o.submit %>
<% end %>