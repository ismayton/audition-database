<% @excerpts.each do |t| %>
    <h4><%= link_to "#{full_title(t)}", excerpt_path(t) %></h4>
    <p>Composed by: <%= t.composer.name %></p>
<% end %>

<% if admin? %>
    <%= link_to "New Excerpt", new_excerpt_path %><br> 
<% end %>